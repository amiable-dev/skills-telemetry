# Copilot emits skills as generic tool calls; the collector maps gen_ai.tool.name -> std.* using this table.
# Generated from skills/ by `make skill-map`; commit alongside the catalogue.
structured-logging:
  policy_ids: logging.required_fields,logging.no_pii
  standard_id: STD-LOG-001
  version: 2.3.0
