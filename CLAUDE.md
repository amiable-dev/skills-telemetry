# skills-telemetry — project context for Claude Code

## What this is
Telemetry and metadata capture for **standards-as-skills**: attribute token cost and outcomes to individual
skills across Claude Code and GitHub Copilot, joined to OPA/Rego policy results, delivery data (Linear/GitHub)
and quality signals. Purpose: a data-driven keep / refine / merge / deprecate loop for skills, and a fair
Claude Code vs Copilot (VS Code) comparison. Full rationale and industry scan: `docs/design-proposal.md`.

Public repo: `github.com/amiable-dev/skills-telemetry`. Metadata only — never emit prompt/response/file content.

## Key design decisions (do not revisit without reason)
- Primary effectiveness metric = **first-time OPA policy pass rate** on the PR, with vs without the skill.
- Custom attributes live in the `std.*` namespace; `gen_ai.*` is normalised at the collector (semconv still
  Development upstream; extend `transform/normalise` rather than changing dashboards).
- Skill identity comes from `SKILL.md` front-matter (`name`, `version`, `standard_id`, `policy_ids`,
  `owner`, `harness_support`, `telemetry`). `stdtel-validate` is the CI contract gate.
- Token attribution: "tail" rule — llm requests after a skill loads until the next skill loads / turn ends;
  `tail_tokens_first_only` kept as a sensitivity check. Hooks always exit 0 (never block the developer).
- Join key `std.ticket.id` is parsed from the git branch at `SessionStart`; unattributed sessions are kept
  for cost analysis, excluded from outcome analysis.
- Copilot skills arrive as generic tool-call spans; `collector/copilot-skill-map.yaml` (generated by
  `make skill-map`, test-enforced) maps them onto `std.skill.*`.

## Layout
`stdtel/` (manifest, state, transcript, exporter, enrich, skillmap, hooks/cli) · `skills/` catalogue ·
`collector/` OTel config · `deploy/` compose stack (collector, Tempo, Prometheus, Grafana, Postgres) ·
`warehouse/` schema + weekly scorecard SQL + Tempo→Postgres loader · `eval/` offline with/without-skill
runner graded by OPA (`--dry-run` in CI) · `tests/` (15 passing, in-memory OTel exporter).

## Commands
`make install` · `make test` · `make validate` · `make skill-map` · `make up` · `make eval-dry`

## Current state / known gaps
- `.claude/settings.json` and the CI workflow (`.github/workflows/ci.yml`) were dot-paths that may be
  missing from the first commit — check and restore.
- `STDTEL_SKILLS_ROOT=skills` in settings is **relative**; must be absolute for use from other projects.
- `_skill_from_input` in `stdtel/hooks/cli.py` guesses the Skill tool's input field names (`skill`/`name`)
  — verify against a real transcript in `~/.claude/projects/`.
- `load_tokens` is a chars/4 heuristic on the Skill tool result.
- Compose stack is unverified (built without Docker access).
- `eval/run_eval.py` expects a `policies/` dir of Rego packages that does not exist yet.

## Next steps (agreed)
1. README: split quick start into "install once globally" vs "onboard a project"; document that
   `~/.claude/settings.json` registers hooks for all projects, per-project overrides go in
   `<project>/.claude/settings.json` (`STDTEL_TEAM`, `STDTEL_HARNESS_MODE`).
2. `_catalogue()` in `hooks/cli.py`: fall back to `~/.claude/skills` when `STDTEL_SKILLS_ROOT` is unset.
3. Onboard a real project: absolute `STDTEL_SKILLS_ROOT`, symlink skills into `~/.claude/skills/`,
   ticket-prefixed branches, verify `~/.stdtel/sessions/<id>.json` then a `std.skill.invocation` span in Tempo.
4. Validate hooks end-to-end against a live Claude Code session; fix field names if needed.
5. Bring up the compose stack and fix whatever breaks.
6. Add a first Rego package under `policies/` so the eval runner works without `--dry-run`.
