# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/

# Local state / results
eval/results.jsonl
.stdtel/
*.jsonl.tmp

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local stack data
deploy/tempo-data/

# local-only credentials for the Langfuse profile
deploy/.env

# Graft runtime cache (session-scoped, churns constantly)
graft/.cache/

# Graft's generated call-graph cards. Regenerable from the source at any time
# (`graft build`), and they churn on every edit, so committing them would put a
# second copy of the codebase in every diff.
/graft/
